import React from 'react';
import { View } from 'react-native';

export default ({ items }) => (
  <View
    style={{
      height: 1,
      backgroundColor: 'green',
      marginTop: 10,
      marginBottom: 10,
    }}
  />
);
